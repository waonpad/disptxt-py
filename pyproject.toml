[tool.poetry]
name = "disptxt"
version = "0.1.0"
description = ""
authors = ["waonpad <waonpad@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
pygame = "^2.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
